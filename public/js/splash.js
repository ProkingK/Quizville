$(document).ready(() => {
    $('.signup-button').click(() => {
        window.location.href = '/signup';
    });
});

$(document).ready(() => {
    $('.login-button').click(() => {
        window.location.href = '/login';
    });
});